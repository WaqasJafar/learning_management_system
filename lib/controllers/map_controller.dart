import 'package:get/get.dart';
import 'package:url_launcher/url_launcher.dart';

class GoogleMapController extends GetxController{

  forDeanOffice() async {
    const url = 'https://goo.gl/maps/AuZxjPguSqdmBtdd8';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  forMedia() async {
    const url = 'https://goo.gl/maps/AuZxjPguSqdmBtdd8';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  forBioloy() async {
    const url = 'https://goo.gl/maps/AuZxjPguSqdmBtdd8';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }
  forNetworkingLab() async {
    const url = 'https://goo.gl/maps/AuZxjPguSqdmBtdd8';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }


  forAdminBlock() async {
    const url = 'https://goo.gl/maps/t7v2TZLuWiVBQ8Ve9';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }


  forCafeteria() async {
    const url = 'https://goo.gl/maps/miQmWqVWc3oj9iSa7';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }


  forGround() async {
    const url = 'https://goo.gl/maps/LkhzWEzozn3Ru6eC6';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  forCricketGround() async {
    const url = 'https://goo.gl/maps/Qwwv22WDDqbEJpWZ8';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }



  forSportsComplex() async {
    const url = 'https://goo.gl/maps/fnE7M3LWPWbuxLgY6';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }


  forFountainPark() async {
    const url = 'https://goo.gl/maps/pHbes44ngx3DkZPm6';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }



  forMasjid() async {
    const url = 'https://goo.gl/maps/LzfRf5KAyxpbEBc5A';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }


  forRebotics() async {
    const url = 'https://goo.gl/maps/FyBrx9qKxLYSCYobA';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }



  forNewBlock() async {
    const url = 'https://goo.gl/maps/mA98i5kEbPYNenez7';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }


  forNewBlock2() async {
    const url = 'https://goo.gl/maps/a8JYLAims54DnKQ27';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  forOldBuilding() async {
    const url = 'https://goo.gl/maps/FziqeY7tWK5Tr3if7';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  forOldBuildingInside() async {
    const url = 'https://goo.gl/maps/yeEhyT2kMFA1SKvFA';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  forOldBuildingOutside() async {
    const url = 'https://goo.gl/maps/RGPa9mKtugD9ujUC9';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  forUsman() async {
    const url = 'https://goo.gl/maps/yvFNGPQpbgrYSUBc6';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

}

